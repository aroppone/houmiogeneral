# Example to create own plugin to Houmio

## Init

Install coffee

Install nodemon

Add sitekey to the prod.env
  
Type:

    npm install
    set -a
    source prod.env
    nodemon site-general.coffee  
    
